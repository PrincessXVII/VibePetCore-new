# VibePetCore 2.6.26 smoke

Date: 2026-06-24
Branch: codex/refactor-gui-pages
Code commit: 92cb806 Fix core repair save rollback

Live controlled GUI smoke:
- Server: Purpur 1.21.11 build 2568
- Test root: `%TEMP%\VibePetCore-smoke-2.6.26`
- Plugin jar: `plugins/VibePetCore-2.6.26.jar`
- Tooling: temporary mineflayer 4.37.1 runtime from `%TEMP%\VibePetCore-bot-smoke`
- Bot script kept in repo: `tools/bot_release_gate_smoke.js`
- Player: `SmokeBot`, offline mode, op level 4

Expected smoke warnings:
- Clean smoke server had no MySQL, so VibePetCore fell back to json storage.
- Smoke server was intentionally started in offline mode on port `25569`.

Verified by live run:
- Direct `ПКМ` on the placed Source while a core was in offhand opened `VibePet - Меню`.
- The same Source click did not summon a runtime pet in parallel.
- `/pet` opened personal GUI, help page opened from slot `17`, and pet growth/info page opened from slot `13`.
- Source quests GUI:
  - `slot 10` from main menu opened quests;
  - gather tab `slot 5` opened correctly;
  - quest in `slot 10` was accepted through GUI;
  - after giving gather resources, the same GUI slot turned the quest in successfully;
  - points changed `0 -> 45`;
  - bread stack dropped to `24`, which matches `64 - 40` for the visible bread gather quest.
- Source forge GUI:
  - forge page opened from main menu `slot 34`;
  - forge attempt button `slot 22` consumed exactly 2 donor `wolf_spawn_egg`;
  - the attempt failed by chance, but the GUI reopened normally and no phantom donor eggs remained.
- Source box GUI:
  - box page opened from main menu `slot 16`;
  - box open button `slot 22` granted a reward egg;
  - total egg count changed `0 -> 1`;
  - reward chat in this run: `Источник открыт: обычная Кот.`
- Post-action audit:
  - `/vpc admin audit SmokeBot` -> `Проблем аудита не найдено.`
  - `/vpc debugpet` still showed intact held `WOLF_SPAWN_EGG` core data.
- Server shutdown through in-game `/stop` was clean; VibePetCore disabled without plugin errors.

Evidence files outside repo:
- bot log: `%TEMP%\VibePetCore-smoke-2.6.26\release-gate-bot.log`
- server log: `%TEMP%\VibePetCore-smoke-2.6.26\release-gate-server-out.log`

Live evolution/repair gate:
- Separate test root: `%TEMP%\VibePetCore-smoke-evolution-2.6.26`
- Bot script kept in repo: `tools/bot_release_evolution_repair_smoke.js`
- Preparation:
  - a clean summon/relog root was created first to persist a real wolf core and matching `PlayerData` pet entry;
  - then the offline `players/<uuid>.json` was edited for the same `petId` to seed:
    - `level=3`
    - `bond=4`
    - `durability=6`
    - one completed evolution quest `evolution_2_hunt` with `boundPetId=<same petId>`
  - this was used only to prove the real GUI spend/save path without grinding bond/quest progress by hand.
- Verified by live run:
  - after `/vpc source set` near the player and a normal offhand summon, `/vpc debugpet` showed `bond=4`, `level=3`, `evolution=1`, `durability=6`;
  - with `24 BONE` and `16 LEATHER`, `/pet -> slot 13 -> slot 22` evolved the wolf successfully;
  - both material stacks were consumed to `0`;
  - `/pet info` changed from `E1` to `E2`;
  - observed post-evolution reset:
    - `level 3 -> 1`
    - `bond 4 -> 0`
    - `durability` stayed `6`
  - code check matches this behavior: `CoreProgressionAPI.tryEvolve(...)` resets level/sublevel/bond/xp on successful stage-up.
  - with the same active pet at `durability=6`, `/pet -> slot 24` consumed exactly `1` totem and raised durability `6 -> 7`;
  - final `/pet info` showed `E2` and `прочность 7/7`;
  - server shutdown through in-game `/stop` was clean; no VibePetCore errors appeared in the server log.
- Evidence files outside repo:
  - bot log: `%TEMP%\VibePetCore-smoke-evolution-2.6.26\gate-bot.log`
  - server log: `%TEMP%\VibePetCore-smoke-evolution-2.6.26\gate-server-out.log`

Release interpretation:
- `Source offhand conflict`, `quest accept/turn-in`, `forge donor spend`, `box reward spend`, `actual evolution button`, and `core repair` now all have live evidence.
- Remaining release uncertainty is reduced to:
  - real proxy/lobby/plugin stack check on the target machine;
  - live TPS/MSPT observation with 2-3 testers and normal plugin neighbors.
