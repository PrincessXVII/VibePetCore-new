# VibePetCore 2.6.33 stack gate report template

Дата:
Тестер:
Сервер:
Jar: `VibePetCore-2.6.33-release-candidate.jar`

## Итог

Статус: `PASS` / `FAIL`

## Роль

Роль теста:
- `Тестер 1 / Source-Economy`
- `Тестер 2 / pet-UX-Repair`
- `Тестер 3 / Forge-Evolution-MSPT`

## Что именно проверялось

Шаги:
1.
2.
3.

## Результат

Что увидели:
- 
- 
- 

## Если PASS

Подтвердить явно:
- не было дубля/потери ядра, donor eggs, тотема, очков;
- GUI/help не врали;
- не было stacktrace;
- MSPT/TPS не дали release-blocker.

## Если FAIL

Обязательно заполнить:
- точный шаг, на котором сломалось;
- что ожидалось;
- что получилось по факту;
- воспроизводится ли повторно: `да / нет`;
- есть ли второй core в `main hand`: `да / нет`;
- был ли активный/core в `offhand`: `да / нет`;
- затронуты ли ресурсы: `ядро / donor eggs / тотем / очки / другое`;
- есть ли stacktrace в логе: `да / нет`.

## Что приложить

Приложения:
- скрин экрана;
- скрин `/pet info` или `/vpc debugpet`, если относится к сценарию;
- кусок server log;
- если это MSPT/TPS: скрин `/mspt` или `/spark tps`.

## Короткий вывод для координатора

Одной строкой:
- `release candidate`
- `needs one P0/P1 fix`
- `blocked only on server stack test`
